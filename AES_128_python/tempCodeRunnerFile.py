tate[0], list):  # Check if state is a list of lists
    #     for row in state:
    #         print(' '.join(f"{byte:02x}" for byte in row))
    # else:  # If state is a single list, print it as one row
    #     print(' '.j